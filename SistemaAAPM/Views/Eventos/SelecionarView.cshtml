@model DataTable
@using System.Data

@{
    ViewData["Title"] = "Eventos";
    Layout = "../Shared/_LayoutADM.cshtml";
}




<div class="container-fluid">

    <br />
    <br />
    <br />
    <br />

    <h2 style="margin-left: 240px" class="title text-center">CONTROLE DE EVENTOS</h2>


    @if (TempData["MsgSucesso"] != null)
    {
        <div class="alert alert-success" style="margin-left: 320px" role="alert">
            @TempData["MsgSucesso"] &emsp;

            <button type="button"
                    class="fechar-alert btn btn-success btn-sm"
                    arial-label="Close">
                X
            </button>
        </div>
    }


    @if (TempData["MsgErro"] != null)
    {
        <div class="alert alert-danger" role="alert">
            @TempData["MsgErro"] &emsp;

            <button type="button"
                    class="fechar-alert btn btn-danger btn-sm"
                    arial-label="Close">
                X
            </button>
        </div>
    }

    <br />



</div>

<br />
<br />
<br />








<div class="ins-btn">
    <div class="row">
        <div class="col-md-12">

            <div class="d-grid gap-2 d-md-flex justify-content-md-start">
                <a class="btn btn-primary" role="button" asp-controller="Eventos"
                   asp-action="InserirExibir"><i class="bi bi-plus"></i> Adicionar</a>
            </div>

        </div>
    </div>
</div>


<br />
<br />
<br />



<div class="fade-in" id="meuElemento">

    <div class="row">
        <div class="col-md-12 d-flex flex-column">

            <table id="table" class="table table-striped teble-houver table-bordered ">
                <thead class="border-3 border-dark rounded-top table-dark">
                    <tr>
                        <th scope="col">Nome Evento</th>
                        <th scope="col">Descrição Evento</th>
                        <th scope="col">Data Evento </th>
                        <th scope="col">Horário Evento </th>
                        <th scope="col">Editar  &emsp; - &emsp;  Excluir</th>
                    </tr>
                </thead>
                <tbody class="border-3 border-dark rounded-bottom">
                    @if (Model != null)
                    {
                        @foreach (DataRow row in Model.Rows)
                        {
                            <tr>
                                <td>@row["Nome_Evento"]</td>
                                <td>@row["Descricao_Evento"]</td>
                                <td>
                                    @if (@row["Data_Evento"] != DBNull.Value)
                                    {
                                        @Convert.ToDateTime(@row["Data_Evento"]).ToString("dd/MM/yyyy")
                                    }
                                </td>
                                <td>
                                    @if (@row["Horario_Evento"] != DBNull.Value)
                                    {
                                        @row["Horario_Evento"]
                                    }
                                </td>
                                <td>
                                    <div class="btn-group" role="group" aria-label="Basic example">
                                        <a role="button " class="btn btn-primary"
                                           asp-controller="Eventos"
                                           asp-action="AlterarExibir"
                                           asp-route-idEvento="@row["Id_Evento"]"><i class="bi bi-pencil"></i></a>

                                        &emsp;
                                        &emsp;
                                        &emsp;

                                        <a role="button " class="btn btn-danger"
                                           asp-controller="Eventos"
                                           asp-action="ExcluirExibir"
                                           asp-route-idEvento="@row["Id_Evento"]"><i class="bi bi-trash-fill"></i></a>
                                    </div>
                                </td>
                            </tr>
                        }
                    }

                </tbody>
            </table>

        </div>
    </div>
</div>
<div />
